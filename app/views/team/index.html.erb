<div class="bg-gray-800 h-full">
    <div class="md:flex md:items-center md:justify-between px-10 py-5 bg-orange-500">
        <div class="flex-1 min-w-0">
            <h2 class="text-2xl font-bold leading-7 text-white sm:text-3xl sm:leading-9 sm:truncate">
                Teams
            </h2>
        </div>
        <div class="mt-4 flex md:mt-0 md:ml-4">
            <span class="ml-3 shadow-sm rounded-md">
                <router-link to="/teams/new">
                    <button type="button"
                        class="inline-flex items-center px-4 py-2 border border-transparent text-sm leading-5 font-medium rounded-md text-white bg-gray-700 hover:bg-gray-600 focus:outline-none focus:shadow-outline-gray focus:border-gray-600 active:bg-gray-600 transition duration-150 ease-in-out">
                        Create New Team
                    </button>
                </router-link>
            </span>
        </div>
    </div>

    <div class="min-h-screen max-w-7xl mx-auto px-4 sm:px-6 md:px-8 py-10">
        <!-- Replace with your content -->

        <% if (@teams.count > 0) %>

        <div class="mt-2 grid gap-5 max-w-lg mx-auto lg:grid-cols-4 lg:max-w-none">
            <% 10.times do %>
            <% @teams.each do |team| %>
            <div class="flex flex-col rounded-lg shadow-lg overflow-hidden transform hover:scale-110 transition duration-150 ease-in-out">
                <div class="flex-shrink-0">
                    <a href="<%= show_team_path(team) %>">
                    <img class="h-48 w-full object-cover"
                        src="<%= url_for(team.cover) %>"
                        alt="" />
                    </a>
                </div>
                <div class="flex-1 bg-white p-3 flex flex-col justify-between">
                    <div class="mt-0 flex items-center">
                        <div class="flex-shrink-0">
                            <a href="<%= show_team_path(team) %>">
                                <img class="h-10 w-10 rounded-full"
                                    src="<%= url_for(team.avatar) %>"
                                    alt="" />
                            </a>
                        </div>
                        <div class="ml-3">
                            <p class="text-sm leading-5 font-medium text-gray-900">
                                <a href="<%= show_team_path(team) %>" class="hover:underline">
                                    <%= team.name %>
                                </a>
                            </p>
                            <div class="flex text-sm leading-5 text-gray-500">
                                <time datetime="2020-03-16">
                                    <%= team.created_at %>
                                </time>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <% end %>
            <% end %>
        </div>

        <div class="bg-white px-4 py-3 flex items-center justify-between border-t border-gray-200 sm:px-6">
            <div class="hidden sm:block">
                <p class="text-sm leading-5 text-gray-700">
                    Showing
                    <span class="font-medium">{{pagingCounter}}</span>
                    to
                    <span class="font-medium">{{ pagingCounter + teams.length -1 }}</span>
                    of
                    <span class="font-medium">{{totalDocs}}</span>
                    results
                </p>
            </div>
            <div class="flex-1 flex justify-between sm:justify-end">
                <button v-if="hasPrevPage" @click="loadPage(prevPage)"
                    class="relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm leading-5 font-medium rounded-md text-gray-700 bg-white hover:text-gray-500 focus:outline-none focus:shadow-outline-blue focus:border-blue-300 active:bg-gray-100 active:text-gray-700 transition ease-in-out duration-150">
                    Previous
                </button>
                <button v-if="hasNextPage" @click="loadPage(nextPage)"
                    class="ml-3 relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm leading-5 font-medium rounded-md text-gray-700 bg-white hover:text-gray-500 focus:outline-none focus:shadow-outline-blue focus:border-blue-300 active:bg-gray-100 active:text-gray-700 transition ease-in-out duration-150">
                    Next
                </button>
            </div>
        </div>
    </div>
    <% else %>
    <div class="border-2 border-dashed border-white text-white text-2xl font-bold rounded-md shadow text-center p-10">
        There are no teams yet. Maybe you want to <a href="/teams/create"
            class="bg-orange-500 rounded-md px-3 py-1">create one</a>?
    </div>
    <% end %>
    <!-- /End replace -->
</div>
</div>